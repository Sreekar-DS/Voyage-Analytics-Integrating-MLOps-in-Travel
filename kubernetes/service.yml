apiVersion: v1
kind: Service
metadata:
  name: voyage-analytics-api-service
  labels:
    app: voyage-analytics-api
spec:
  selector:
    app: voyage-analytics-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
